import nodeFs from "node:fs/promises";
import type { CompilationResult } from "./compile.js";
export declare function seedPreviousCompilationFromOutdir(args: {
    outdir: string;
    fs?: typeof nodeFs;
    cwd?: string;
}): Promise<CompilationResult | undefined>;
