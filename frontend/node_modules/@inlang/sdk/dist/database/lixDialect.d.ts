import { PostgresAdapter, PostgresIntrospector, PostgresQueryCompiler, type CompiledQuery, type Dialect, type Driver, type Kysely } from "kysely";
import type { Lix, LixBatchStatement } from "@lix-js/sdk";
/** Kysely bridge for Lix's DataFusion SQL interface. */
export declare class LixDialect implements Dialect {
    #private;
    constructor(lix: Lix);
    createDriver(): Driver;
    createQueryCompiler(): PostgresQueryCompiler;
    createAdapter(): PostgresAdapter;
    createIntrospector(db: Kysely<unknown>): PostgresIntrospector;
}
/** Convert Kysely's compiled query to the one Lix SQL/parameter contract. */
export declare function compileLixQuery(compiledQuery: CompiledQuery): LixBatchStatement;
//# sourceMappingURL=lixDialect.d.ts.map