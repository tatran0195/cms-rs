import type { Kysely } from "kysely";
import type { BundleNested, InlangDatabaseSchema } from "../database/schema.js";
/**
 * Select bundles with nested messages and variants.
 *
 * Lix uses DataFusion, which does not provide SQLite's json_group_array()
 * helper. A flat left join keeps this to one engine round-trip and the small
 * reconstruction below preserves the established SDK result shape.
 */
export declare const selectBundleNested: (db: Kysely<InlangDatabaseSchema>) => {
    where(column: "bundle.id", operator: "=", value: string): /*elided*/ any;
    selectAll(): /*elided*/ any;
    execute(): Promise<BundleNested[]>;
    executeTakeFirst(): Promise<BundleNested | undefined>;
    executeTakeFirstOrThrow(): Promise<BundleNested>;
};
//# sourceMappingURL=selectBundleNested.d.ts.map