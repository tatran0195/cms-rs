function escape(url) {
    const bytes = new TextEncoder().encode(url);
    // 64-bit FNV1a hash to make the file-names shorter
    // https://en.wikipedia.org/wiki/FNV-1a
    const hash = bytes.reduce((hash, byte) => BigInt.asUintN(64, (hash ^ BigInt(byte)) * 1099511628211n), 14695981039346656037n);
    return hash.toString(36);
}
async function readModuleFromCache(moduleURI, lix) {
    const moduleHash = escape(moduleURI);
    const filePath = `/cache/plugins/${moduleHash}`;
    const result = await lix.execute("SELECT content FROM lix_file WHERE path = $1", [filePath]);
    const file = result.rows[0];
    if (file) {
        return new TextDecoder().decode(file.value("content").asBytes());
    }
    return undefined;
}
async function writeModuleToCache(moduleURI, moduleContent, lix) {
    const moduleHash = escape(moduleURI);
    const filePath = `/cache/plugins/${moduleHash}`;
    await lix.execute("INSERT INTO lix_file (path, content) VALUES ($1, $2) ON CONFLICT (path) DO UPDATE SET content = excluded.content", [filePath, new TextEncoder().encode(moduleContent)]);
}
/**
 * Implements a "Network-First" caching strategy.
 */
export function withCache(moduleLoader, lix) {
    return async (uri) => {
        try {
            const moduleAsText = await moduleLoader(uri);
            await writeModuleToCache(uri, moduleAsText, lix);
            return moduleAsText;
        }
        catch (e) {
            // network fetch failed, try to read from cache
            const cacheResult = await readModuleFromCache(uri, lix);
            if (cacheResult) {
                return cacheResult;
            }
            else {
                throw e;
            }
        }
    };
}
//# sourceMappingURL=cache.js.map