/// <reference lib="webworker" />
import { startWorkerHost } from "./host.js";
const scope = globalThis;
startWorkerHost({
    postMessage(message) {
        scope.postMessage(message);
    },
    onMessage(listener) {
        scope.onmessage = (event) => listener(event.data);
    },
});
